# 第70题 爬楼梯

```javascript
/**
 * @param {number} n
 * @return {number}
 */
var climbStairs = function(n) {
    return 1 / (sqrt5 = Math.sqrt(5)) * (Math.pow((1 + sqrt5) / 2, n + 1) - Math.pow((1 - sqrt5) / 2, n + 1));
};
```

